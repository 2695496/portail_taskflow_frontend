
import Aside from "@/components/Aside";
import Header from "@/components/Header";
import AsideInfirmier from "@/components/aside/AsideInfirmier";
import INF_Accueil from "@/components/contents/inf/INF_Accueil";
import INF_Content from "@/components/contents/inf/INF_Content";
import Head from "next/head";
import { useState } from "react";
import { useRouter } from "next/router";
import { routes } from "./services/routes.js";
import { useEffect } from "react";

export default function Infirmier() {
  const [content, setContent] = useState(<INF_Accueil />);
  const router = useRouter();
  const { pathname } = router;

  
  const [visible, setVisible] = useState(false);
  const [titre, setTitre] = useState('Acceuil');
  useEffect(() => {
    const token = localStorage.getItem("token");
    if (!token) {
      router.push("/login");
    } else {
      const user = JSON.parse(token);
      const route_user = routes(user.id_role);
      if (pathname !== route_user) {
        router.push(route_user);
      } else {
        setVisible(true);
      }
    }
  }, []);
  return (
    <>
      {visible ? (
        <>
          <Head>
            <title>Infirmier - {titre}</title>
            <meta name="description" content="Generated by create next app" />
            <meta
              name="viewport"
              content="width=device-width, initial-scale=1"
            />
            <link rel="icon" href="/favicon.ico" />
          </Head>

          <Header />
          <Aside  asideContent={<AsideInfirmier setContent={setContent} setTitre={setTitre} />} />
          <INF_Content content={content}/>
        </>
      ):(null)}
    </>
  );
}

